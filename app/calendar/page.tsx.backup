'use client';

import { useState, useEffect } from 'react';
import { useRouter } from 'next/navigation';

interface CalendarEvent {
    id: string;
    date: string;
    time: string;
    title: string;
    country: string;
    importance: number;
}

export default function CalendarPage() {
    const router = useRouter();
    const [events, setEvents] = useState<CalendarEvent[]>([]);
    const [loading, setLoading] = useState(true);
    const [currentMonth, setCurrentMonth] = useState(new Date());

    useEffect(() => {
        fetchEvents();
    }, []);

    const fetchEvents = async () => {
        try {
            const res = await fetch('/api/calendar');
            const data = await res.json();
            setEvents(data.schedule || []);
        } catch (error) {
            console.error('Failed to fetch events:', error);
        } finally {
            setLoading(false);
        }
    };

    const getStars = (importance: number) => {
        return '⭐'.repeat(importance);
    };

    const getDaysInMonth = () => {
        const year = currentMonth.getFullYear();
        const month = currentMonth.getMonth();
        const firstDay = new Date(year, month, 1);
        const lastDay = new Date(year, month + 1, 0);
        const daysInMonth = lastDay.getDate();
        const startDayOfWeek = firstDay.getDay();

        const days = [];
        // Empty cells for days before month starts
        for (let i = 0; i < startDayOfWeek; i++) {
            days.push(null);
        }
        // Days of the month
        for (let day = 1; day <= daysInMonth; day++) {
            days.push(day);
        }
        return days;
    };

    const isToday = (day: number | null) => {
        if (!day) return false;
        const today = new Date();
        return (
            day === today.getDate() &&
            currentMonth.getMonth() === today.getMonth() &&
            currentMonth.getFullYear() === today.getFullYear()
        );
    };

    const getEventsForDay = (day: number | null) => {
        if (!day) return [];
        const dateStr = `${currentMonth.getFullYear()}-${String(currentMonth.getMonth() + 1).padStart(2, '0')}-${String(day).padStart(2, '0')}`;
        return events.filter(event => event.date === dateStr);
    };

    const getTodayEvents = () => {
        const today = new Date().toISOString().split('T')[0];
        return events.filter(event => event.date === today);
        style = {{
            fontSize: '1.5rem',
                marginRight: '1rem',
                    background: 'white',
                        border: 'none',
                            borderRadius: '50%',
                                width: '40px',
                                    height: '40px',
                                        cursor: 'pointer',
                                            boxShadow: '0 2px 8px rgba(0,0,0,0.15)'
        }
    }
                    >
                        ←
                    </button >
        <h1 style={{ fontSize: '2rem', color: 'white', fontWeight: 'bold' }}>📅 경제 캘린더</h1>
                </div >

    {
        loading?(
                    <div style = {{
                textAlign: 'center',
                padding: '4rem',
                background: 'white',
                borderRadius: '1rem',
                boxShadow: '0 8px 24px rgba(0,0,0,0.12)'
                    }} >
                        <div style={{ fontSize: '3rem', marginBottom: '1rem' }}>📆</div>
                        <p style={{ color: '#666' }}>캘린더를 불러오는 중...</p>
                    </div >
                ) : (
        <>
            {/* Calendar Grid */}
            <div style={{
                background: 'white',
                borderRadius: '1rem',
                padding: '1.5rem',
                marginBottom: '2rem',
                boxShadow: '0 8px 24px rgba(0,0,0,0.12)'
            }}>
                {/* Month Header */}
                <div style={{
                    display: 'flex',
                    justifyContent: 'space-between',
                    alignItems: 'center',
                    marginBottom: '1.5rem'
                }}>
                    <button
                        onClick={() => setCurrentMonth(new Date(currentMonth.getFullYear(), currentMonth.getMonth() - 1))}
                        style={{
                            background: 'linear-gradient(135deg, #4facfe 0%, #00f2fe 100%)',
                            color: 'white',
                            border: 'none',
                            borderRadius: '50%',
                            width: '36px',
                            height: '36px',
                            cursor: 'pointer',
                            fontSize: '1.2rem'
                        }}
                    >
                        ‹
                    </button>
                    <h2 style={{ fontSize: '1.5rem', fontWeight: 'bold', color: '#1f1f1f' }}>
                        {currentMonth.getFullYear()}년 {monthNames[currentMonth.getMonth()]}
                    </h2>
                    <button
                        onClick={() => setCurrentMonth(new Date(currentMonth.getFullYear(), currentMonth.getMonth() + 1))}
                        style={{
                            background: 'linear-gradient(135deg, #4facfe 0%, #00f2fe 100%)',
                            color: 'white',
                            border: 'none',
                            borderRadius: '50%',
                            width: '36px',
                            height: '36px',
                            cursor: 'pointer',
                            fontSize: '1.2rem'
                        }}
                    >
                        ›
                    </button>
                </div>

                {/* Day Names */}
                <div style={{
                    display: 'grid',
                    gridTemplateColumns: 'repeat(7, 1fr)',
                    gap: '0.5rem',
                    marginBottom: '0.5rem'
                }}>
                    {dayNames.map(day => (
                        <div key={day} style={{
                            textAlign: 'center',
                            fontWeight: 'bold',
                            color: '#666',
                            fontSize: '0.875rem',
                            padding: '0.5rem'
                        }}>
                            {day}
                        </div>
                    ))}
                </div>

                {/* Calendar Days */}
                <div style={{
                    display: 'grid',
                    gridTemplateColumns: 'repeat(7, 1fr)',
                    gap: '0.5rem'
                }}>
                    {getDaysInMonth().map((day, index) => {
                        const dayEvents = getEventsForDay(day);
                        const today = isToday(day);

                        return (
                            <div
                                key={index}
                                style={{
                                    minHeight: '80px',
                                    padding: '0.5rem',
                                    background: today ? 'linear-gradient(135deg, #FFD700, #FFA500)' : day ? 'white' : 'transparent',
                                    border: day ? '2px solid #f0f0f0' : 'none',
                                    borderRadius: '0.5rem',
                                    position: 'relative',
                                    cursor: day ? 'pointer' : 'default',
                                    transition: 'all 0.2s'
                                }}
                                onMouseEnter={(e) => {
                                    if (day && !today) {
                                        e.currentTarget.style.background = '#f8f8f8';
                                        e.currentTarget.style.transform = 'scale(1.05)';
                                    }
                                }}
                                onMouseLeave={(e) => {
                                    if (day && !today) {
                                        e.currentTarget.style.background = 'white';
                                        e.currentTarget.style.transform = 'scale(1)';
                                    }
                                }}
                            >
                                {day && (
                                    <>
                                        <div style={{
                                            fontWeight: today ? 'bold' : 'normal',
                                            color: today ? 'white' : '#1f1f1f',
                                            fontSize: '0.9rem',
                                            marginBottom: '0.25rem'
                                        }}>
                                            {day}
                                        </div>
                                        {dayEvents.slice(0, 2).map(event => (
                                            <div key={event.id} style={{
                                                fontSize: '0.65rem',
                                                background: today ? 'rgba(255,255,255,0.3)' : 'rgba(240,93,251,0.1)',
                                                padding: '0.15rem 0.25rem',
                                                borderRadius: '0.25rem',
                                                marginBottom: '0.15rem',
                                                overflow: 'hidden',
                                                textOverflow: 'ellipsis',
                                                whiteSpace: 'nowrap',
                                                color: today ? 'white' : '#666'
                                            }}>
                                                {getStars(event.importance)} {event.time}
                                            </div>
                                        ))}
                                        {dayEvents.length > 2 && (
                                            <div style={{ fontSize: '0.6rem', color: today ? 'white' : '#999' }}>
                                                +{dayEvents.length - 2}개
                                            </div>
                                        )}
                                    </>
                                )}
                            </div>
                        );
                    })}
                </div>
            </div>

            {/* Today's Events List */}
            <div style={{
                background: 'white',
                borderRadius: '1rem',
                padding: '1.5rem',
                boxShadow: '0 8px 24px rgba(0,0,0,0.12)'
            }}>
                <h2 style={{ fontSize: '1.5rem', fontWeight: 'bold', color: '#1f1f1f', marginBottom: '1rem' }}>
                    ⭐ 오늘의 주요 일정
                </h2>

                {getTodayEvents().length === 0 ? (
                    <div style={{ textAlign: 'center', padding: '2rem', color: '#999' }}>
                        <div style={{ fontSize: '3rem', marginBottom: '0.5rem' }}>📭</div>
                        <p>오늘은 예정된 경제 일정이 없습니다.</p>
                    </div>
                ) : (
                    <div style={{ display: 'flex', flexDirection: 'column', gap: '1rem' }}>
                        {getTodayEvents().map((event, index) => (
                            <div
                                key={event.id}
                                style={{
                                    padding: '1.25rem',
                                    background: 'linear-gradient(135deg, #f8f9fa, #fff)',
                                    border: '2px solid #f0f0f0',
                                    borderRadius: '0.75rem',
                                    transition: 'all 0.3s',
                                    animation: `slideIn 0.4s ease-out ${index * 0.1}s backwards`
                                }}
                                onMouseEnter={(e) => {
                                    e.currentTarget.style.transform = 'translateY(-2px)';
                                    e.currentTarget.style.boxShadow = '0 8px 20px rgba(0,0,0,0.1)';
                                }}
                                onMouseLeave={(e) => {
                                    e.currentTarget.style.transform = 'translateY(0)';
                                    e.currentTarget.style.boxShadow = 'none';
                                }}
                            >
                                <div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: '0.5rem' }}>
                                    <span style={{
                                        fontSize: '1.5rem',
                                    }}>
                                        {getStars(event.importance)}
                                    </span>
                                    <span style={{
                                        background: 'linear-gradient(135deg, #4facfe 0%, #00f2fe 100%)',
                                        color: 'white',
                                        padding: '0.25rem 0.75rem',
                                        borderRadius: '9999px',
                                        fontSize: '0.875rem',
                                        fontWeight: 'bold'
                                    }}>
                                        {event.time}
                                    </span>
                                </div>
                                <h3 style={{ fontSize: '1.125rem', fontWeight: 'bold', color: '#1f1f1f', marginBottom: '0.5rem' }}>
                                    {event.title}
                                </h3>
                                <div style={{ color: '#666', fontSize: '0.875rem' }}>
                                    📍 {event.country}
                                </div>
                            </div>
                        ))}
                    </div>
                )}
            </div>

            <style jsx>{`
              @keyframes slideIn {
                from {
                  opacity: 0;
                  transform: translateY(20px);
                }
                to {
                  opacity: 1;
                  transform: translateY(0);
                }
              }
            `}</style>
        </>
    )
}
            </div >
        </div >
    );
}
